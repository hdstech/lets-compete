meta {
  name: QA3 - Participant Self-Register + Approve + Identity
  seq: 6
}

docs {
  Runs after activate_event (T6) but before the first reveal (QA5), so the QA5 roster-freeze trigger is not yet in effect and this folder is unaffected by the T6/T8a/QA4 reordering discussed above.
}
