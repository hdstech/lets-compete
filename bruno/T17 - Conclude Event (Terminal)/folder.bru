meta {
  name: T17 - Conclude Event (Terminal)
  seq: 17
}

docs {
  Ships conclude_event, the last lifecycle transition. This is the final folder in the collection's main path — a full pass through Auth -> ... -> T17 exercises every RPC shipped by QA0 through T17 (QA12's tiebreak sub-flow excepted, per that folder's own note).
}
